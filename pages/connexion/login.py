from tkinter import *


def Login(windows,width, height):
    page_login = Canvas(windows,height=height, width=width)

    # titre de la page
    Label(page_login,text="Login",font=('arial',20,'bold')).place(x=50,y=50)






    page_login.place(x=0,y=0)